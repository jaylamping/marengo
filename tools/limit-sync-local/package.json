{
  "name": "limit-sync-local",
  "private": true,
  "type": "module",
  "description": "Loopback Set Limits → local git checkout via marengo-limit-sync",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": "^24.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
