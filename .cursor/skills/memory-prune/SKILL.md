# Memory Prune — DISABLED

**Status: disabled (2026-07-14)** with mem0.

Do not run `tools/mem0-mcp/dist/prune.js` or the `memory-prune-evening` automation against a live mem0 instance.

When mem0 is re-enabled, restore prune policy from git history and [docs/mem0-ops.md](../../../docs/mem0-ops.md).

For local continuity, keep `.omo/session-handoff.md` short and archive dated copies only when useful — no automated prune required.
