Wave 0 / Todo 0 — Safety Gate Verification

Date: 2026-06-19
Session: ses_current
Worker: Sisyphus-Junior (deepseek/deepseek-v4-flash)
Verifier: Sisyphus (orchestrator, independent)

Changed files:
- Cargo.toml (root) — added proptest = "1" to [workspace.dependencies]
- crates/berthier/Cargo.toml — added proptest = { workspace = true } to [dev-dependencies]
- crates/armee-dynamics/tests/golden_tau_g.rs — NEW, 7 golden-value tests
- crates/berthier/tests/sign_cross_crate.rs — NEW, 3 cross-crate sign tests

Verification results:
1. cargo test -p armee-dynamics --test golden_tau_g → 7/7 PASS (verified)
   - golden_tau_g_at_zero, _at_0_3, _at_neg_0_3, _at_0_785, _at_pi_over_2, _symmetric, _at_neg_0_5
2. cargo clippy -p armee-dynamics --all-targets -- -D warnings → CLEAN (verified)
3. cargo test -p berthier --test sign_cross_crate → BLOCKED on Windows (pre-existing chappe Unix socket issue, not caused by changes). Will run on Linux CI.
4. cargo deny check → not installed on Windows host (skipped, non-blocking)
5. File existence verified: golden_tau_g.rs, sign_cross_crate.rs, proptest in both Cargo.toml files

AdversarialVerify verdict: confirmed (with caveat)
- Golden tests: confirmed (7/7 pass, independently verified)
- Sign test: code review confirmed (structurally correct, uses MemoryBus + Supervisor + CAN frame decode), but runtime verification deferred to Linux CI due to pre-existing Windows compile issue
- proptest dep: confirmed (present in both Cargo.toml files, cargo tree shows v1.11.0)

Caveat: sign_cross_crate.rs runtime verification pending Linux CI. This is acceptable because:
- The compile failure is pre-existing (chappe uses Unix sockets, all berthier tests have this issue on Windows)
- The test code follows the exact pattern from existing davout tests
- The armee-dynamics golden tests (which don't depend on chappe) all pass

Risks:
- Golden values are PRE-COM-correction; must be updated after Todo 6
- sign_cross_crate.rs untested on Windows (will run on CI)
