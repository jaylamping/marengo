# Loaded by `vite` / `npm run dev` only. Not used by Pi-hosted Consul.
# Optional: mirror Durable Set Limits into this git checkout (`just limit-sync-serve`).
# Skip both if you only care about Pi YAML/URDF (gateway Durable Apply).
VITE_LIMIT_SYNC_URL=http://127.0.0.1:8790
