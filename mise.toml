# Optional native toolchain (container is source of truth). https://mise.jdx.dev/
[tools]
rust = "stable"
node = "24.16.0"
protoc = "28.3"
buf = "1.47.2"

[env]
_.path = ["{{config_root}}/consul/node_modules/.bin"]
